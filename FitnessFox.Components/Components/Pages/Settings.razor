@page "/settings"
@using FitnessFox.Components.Services
@inject IGoogleSyncService googleSyncService
<h3>Settings</h3>

<MudButton OnClick="Sync">Sync to Google</MudButton>
@code {
    public async Task Sync()
    {
        await googleSyncService.Sync();
    }
}
